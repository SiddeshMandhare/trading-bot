services:
  - type: web
    name: trading-bot
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: PORT
        value: 5000
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    autoDeploy: true